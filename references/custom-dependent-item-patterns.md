# Custom dependent item patterns

Use one master item for a multi-metric JSON/XML/Prometheus/SNMP payload and split into dependent items with preprocessing. Keep raw master history short if payloads are large.
