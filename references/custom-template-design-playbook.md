# Custom template design playbook

Create original templates from monitoring goals. Use macros, tags, value maps, LLD, dependent items, preprocessing, dashboards, and trigger dependencies. Avoid shell-first design.
