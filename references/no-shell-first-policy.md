# No shell first policy

Do not create shell scripts as the first template design response. Use shell/UserParameter only when native Zabbix item types cannot solve the metric and document the reason.
