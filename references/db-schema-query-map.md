# DB schema query map

Common metadata tables: `hosts`, `items`, `interface`, `hosts_groups`, `hstgrp`, `triggers`, `functions`, `events`, `problem`.

Hot tables: `history*`, `trends*`, `events`, `problem`. Always filter hot reads by time and IDs.
