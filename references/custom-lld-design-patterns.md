# Custom LLD design patterns

Use LLD for dynamic resources: interfaces, filesystems, queues, tenants, containers, SSL endpoints, DB instances, cloud resources. Use stable `{#MACRO}` names, filters, prototypes, and overrides.
