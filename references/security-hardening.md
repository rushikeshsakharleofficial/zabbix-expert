# Security hardening

Use TLS/PSK or certificates for agents/proxies, least-privilege RBAC, protected secret macros, read-only DB users for reporting, API tokens with minimal permissions, and audit logging.
