# Custom preprocessing patterns

Use JSONPath, XPath, regex, Prometheus parsing, change per second, multipliers, boolean conversion, discard unchanged with heartbeat, and custom-on-fail handling before storage.
