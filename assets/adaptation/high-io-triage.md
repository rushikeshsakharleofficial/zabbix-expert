# High I/O triage

Run read-only checks: `zabbix_server -R diaginfo`, queue details, internal items, `vmstat`, `iostat`, DB slow queries, locks, retention/housekeeping, proxy backlog, unsupported items.
